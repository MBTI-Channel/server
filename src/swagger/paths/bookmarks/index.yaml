post:
  tags:
    - bookmarks
  description: 북마크 등록
  security:
    - bearerAuth: []
  requestBody:
    content:
      application/json:
        schema:
          type: object
          properties:
            targetId:
              type: integer
              example: 45
  responses:
    $ref: "../../responses/bookmarks/index.post.yaml"

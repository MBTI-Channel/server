get:
  tags:
    - users
  description: 내가 참여한 공식 질문 조회
  parameters:
    - name: Bearer 토큰
      in: header
      required: true
      type: string
      example: asduehfinnncudI1NiIsInR5csdwd665s88db
    - name: Refresh 토큰
      in: cookie
      required: true
      type: string
      example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9
  responses:
    $ref: "../../responses/users/id.survey.get.yaml"

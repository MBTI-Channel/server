"200":
  description: access token 유효함
  content:
    application/json:
      schema:
        type: object
        properties:
          message:
            type: string
            example: access token valid
          id:
            type: integer
            example: 1

"401":
  description: authentication error

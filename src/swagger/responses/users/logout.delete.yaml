"204":
  description: 로그아웃 성공

"401":
  description: access token 또는 refresh token이 유효하지 않음
  content:
    application/json:
      schema:
        type: object
        properties:
          message:
            type: string
            example: authentication error
